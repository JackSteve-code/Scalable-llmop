
import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

const sidebars: SidebarsConfig = {
  tutorialSidebar: [
    'Scalable LLMOps Pipeline', // This should be your Scalable LLMOps Pipeline file
    // Add any other specific doc IDs here directly
  ],
};

export default sidebars;